name: this-is-how-you-pwn-the-time-war
author: aplet123
description: |-
  Delete before solving.

  `{{nc}}`

flag:
  file: flag.txt

provide:
  - ./pwn_the_time_war.c
  - Dockerfile
  - ./pwn_the_time_war

containers:
  main:
    build: .
    replicas: 1
    ports:
      - 5000
    securityContext:
      privileged: true

expose:
  main:
  - target: 5000
    tcp: 31313
